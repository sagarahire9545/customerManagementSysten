package com.custManagent.auth;


//public class JwtValidation {
//	
//	
////	
////	private static String authentication(String token) {
////        try {
////            // Validate the token and return "SUCCESS" if valid
////            if (isTokenExpired(token)) {
////                return "FAILURE"; // Return failure if the token is expired
////            }
////            return "SUCCESS"; // If valid, return "SUCCESS"
////        } catch (Exception e) {
////            return "FAILURE"; // Return failure if any exception occurs during validation
////        }
////    }
//
//}
